# Copy to `.env` and adjust. Only EXPO_PUBLIC_* vars are readable in the app,
# and they are PUBLIC (inlined into the bundle) — never put secrets here.

# Toggle analytics event delivery. Defaults to off in dev, on in release.
EXPO_PUBLIC_ANALYTICS_ENABLED=false
